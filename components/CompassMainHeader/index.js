// CompassMainHeader component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "CompassMainHeader";
export const componentDescription = "The wrapper component for header content in the main Compass area. When building out a custom implementation,\nyou should ensure any content within the main header is rendered inside a Panel, PanelMain, PanelMainBody stack and main header content wrappers.";
export const propsCount = 5;
export const requiredProps = [];
