// CompassMainHeaderTitle component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "CompassMainHeaderTitle";
export const componentDescription = "A wrapper component for custom title content to be passed into a Compass main header. This should also be wrapped\nby a Compass main header content component.";
export const propsCount = 2;
export const requiredProps = ["children"];
