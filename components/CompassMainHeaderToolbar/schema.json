{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "title": "CompassMainHeaderToolbar Props",
  "description": "A wrapper component for custom toolbar content to be passed into a Compass main header. This should also be wrapped\nby a Compass main header content component.",
  "properties": {
    "children": {
      "description": "Content of the main header toolbar. (React component or element)"
    },
    "className": {
      "description": "Additional classes added to the main header toolbar.",
      "type": "string"
    }
  },
  "required": [
    "children"
  ],
  "additionalProperties": false
}