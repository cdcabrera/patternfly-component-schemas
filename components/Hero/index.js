// Hero component metadata
import schema from './schema.json' assert { type: 'json' };

export default schema;
export { schema };

// Component metadata
export const componentName = "Hero";
export const componentDescription = "The main Hero component that allows adjusting of its background images and gradients in different color modes (such as light and dark).";
export const propsCount = 9;
export const requiredProps = [];
